<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rtse.japantourguide" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".schedule.MainScheduleActivity" />
        <activity android:name=".memo.MainMemoActivity"/>
        <activity android:name=".memo.memo_new"/>
        <activity android:name=".memo.memo_check"/>
        <activity android:name=".memo.memo_edit"/>

        <activity android:name=".schedule.schedule_new"/>
        <activity android:name=".schedule.schedule_check"/>
        <activity android:name=".schedule.schedule_edit"/>



    </application>

</manifest>
